# Katalog roboczy - gdzie znajduje sie .claude/ ktorym zarzadzamy
WORKSPACE_DIR=/sciezka/do/twojego/projektu

# === TRYB DZIALANIA - trzy opcje, EXPOSURE i HOST musza sie zgadzac ===
#
# local (domyslny) - tylko ta maszyna, HOST musi zostac 127.0.0.1
# lan             - inne urzadzenia w Twojej sieci lokalnej, bez Caddy
# world           - dostep z internetu przez Twoja domene, przez Caddy
#
EXPOSURE=lan
#
# ZMIEN ponizszy adres na SWOJ realny adres LAN serwera (sprawdzisz:
# `ip addr` / `hostname -I`). Sam '192.168.1.100' to tylko przyklad do zmiany
# przy pierwszym uruchomieniu.
HOST=192.168.1.100
PORT=4200
#
# --- Gdy zechcesz przejsc na EXPOSURE=world (dostep z internetu) ---
# 1. Zmien powyzej: EXPOSURE=world, HOST=127.0.0.1 (node ma nasluchiwac
#    WYLACZNIE lokalnie na serwerze - to Caddy odbiera ruch z internetu).
# 2. Dodaj w konfiguracji Caddy osobna subdomene, np.:
#
#      panel.twojadomena.pl {
#          reverse_proxy 127.0.0.1:4200
#      }
#
#    Caddy sam zalatwi certyfikat TLS (Let's Encrypt) - nic wiecej nie trzeba
#    robic. Podmien "panel.twojadomena.pl" na docelowa subdomene.

# Haslo do panelu logowania zastapione kontami systemowymi (PAM) - patrz
# AUTH_USERS ponizej. Sekret do podpisywania sesji nadal jest potrzebny.
# WYMAGANE dla EXPOSURE=lan i EXPOSURE=world - serwer odmowi startu bez tego,
# bo bez logowania kazdy w Twojej sieci/na Twojej subdomenie mialby pelny
# dostep do zapisu/kasowania plikow .claude/, crontabu i terminala CLI.

# Whitelist kont systemowych ktore MOGA sie zalogowac (login+haslo takie jak
# przy SSH). Kilka loginow oddzielonych przecinkiem, np. AUTH_USERS=zibi,ania
# Tylko te konta - poprawny login+haslo systemowe spoza tej listy NIE zaloguje.
#
# WYMAGANIA SYSTEMOWE (patrz README, sekcja "Logowanie systemowe (PAM)"):
# - proces node musi byc rootem ALBO czlonkiem grupy `shadow`, inaczej PAM
#   pozwoli sprawdzac tylko haslo wlasnego uzytkownika procesu, nie kont
#   z tej listy.
AUTH_USERS=zmien-na-swoj-login,opcjonalnie-drugi-login

# Sekret do podpisywania sesji logowania (zawsze wymagany).
# Wygeneruj: openssl rand -hex 32
SESSION_SECRET=zmien-to-na-losowy-64-znakowy-hex

# Opcjonalne dodatkowe zawezenie CORS - w tej architekturze (frontend i API
# serwowane z tego samego originu) praktycznie niepotrzebne. Zostaw puste,
# chyba ze wiesz ze chcesz odpytywac API z innej domeny.
ALLOWED_ORIGIN=

# Opcjonalnie: nazwa uslugi PAM uzywanej do logowania (domyslnie "login",
# czyli standardowy stos PAM jak przy logowaniu na konsoli/SSH).
# PAM_SERVICE=login

# Opcjonalnie: gdzie Claude Code trzyma dane sesji, jesli zmieniles domyslna
# lokalizacje przez CLAUDE_CONFIG_DIR w swoim srodowisku (domyslnie ~/.claude).
# CLAUDE_CONFIG_DIR=/home/twoj-user/.claude

